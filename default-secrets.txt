# ─────────────────────────────────────────────────────────────
#  PII Guardrail — what to protect
#
#  Edit this file to change what gets blocked. Changes take
#  effect on your next prompt; no restart needed.
#
#  This file lives in your plugin data directory and SURVIVES
#  plugin updates. The copy inside the plugin is only a template.
#
#  Syntax:
#    word          enable a built-in detector
#    "literal"     block this exact string anywhere
#    /regex/       block anything matching your pattern
#    !text         allowlist — never flag anything containing this
#    # comment     ignored
# ─────────────────────────────────────────────────────────────


# ── Credentials ──────────────────────────────────────────────
#  High confidence. The format itself is the evidence, so these
#  rarely misfire. Recommended: leave all of these on.

aws_access_key
aws_secret_key
github_token
anthropic_key
openai_key
slack_token
google_api_key
stripe_key
jwt
private_key
db_uri


# ── Personal data ────────────────────────────────────────────

email


# ── Noisy — off by default ───────────────────────────────────
#  These are correct but fire constantly in a coding context.
#  Uncomment deliberately, and expect false positives.

#  credit_card     any Luhn-valid 13-19 digit number; long
#                  numeric IDs can collide
#  phone           version strings and numeric IDs look like
#                  phone numbers
#  ip_address      public IPs only, but CDN and cloud addresses
#                  in logs will match
#  dob             any ISO or DD/MM/YYYY date — changelogs,
#                  migration filenames, test fixtures
#  generic_secret  matches `token = "..."` style assignments;
#                  also matches git SHAs, lockfile hashes, UUIDs

# credit_card
# phone
# ip_address
# dob
# generic_secret


# ── Literal values ───────────────────────────────────────────
#  "acme-internal.corp"
#  "project-codename"


# ── Custom patterns ──────────────────────────────────────────
#  /EMP-[0-9]{6}/


# ── Allowlist ────────────────────────────────────────────────
#  !example.com
#  !noreply.github.com
#  !127.0.0.1
